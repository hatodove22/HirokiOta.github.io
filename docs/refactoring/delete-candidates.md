# 削除候補リスト（2025-11-05 時点案）

| パス | 種類 | 役割メモ / 現状 | 影響懸念度 | コメント |
| --- | --- | --- | --- | --- |
| `tmp/` | 一時ディレクトリ | 作業時に生成されたテンポラリが残存 | ★☆☆（低） | プロジェクト本体から参照されず、削除しても再生成可能。 |
| `build/` | ビルド成果物 | 旧ビルド出力の残骸 | ★☆☆（低） | 現行 Vite は `dist/` を使用。削除前に `npm run build` で再生成可能なことを確認。 |
| `dist/` | ビルド成果物 | 直近のビルド出力 | ★☆☆（低） | 本番デプロイを GitHub Actions 等で行う場合は不要。手動配布の用途があるなら要バックアップ。 |
| `api/` | 空ディレクトリ | 現状ファイルなし | ★☆☆（低） | 参照箇所がなければ即削除可。 |
| `tmp_devchrome_lh.json` | ログファイル | Chrome Lighthouse 計測結果？ | ★☆☆（低） | レポートが必要なら `docs/` 等へ移動、それ以外は削除可。 |
| `test-content-loader.js` | スクリプト | 旧テスト用ローダー | ★★☆（中） | 参照が無ければ削除。再利用予定がある場合は `scripts/` へ整理。 |
| `ersyamaoOneDrive太田ポートフォリオ整備博士課程学生ポートフォリオ V1Portfolio_EditSystem_Proto/` | 誤作成ディレクトリ | OneDrive 同期時の誤生成 | ★☆☆（低） | 完全に孤立していれば削除して問題なし。 |
| `test.txt` | テストファイル | 一時検証で作成されたテキスト | ★☆☆（低） | 中身確認のうえ不要なら削除。 |
| `private_docs/` | 文書・アーカイブ | 個人バックアップや参考資料 | ★★☆（中） | コンテンツ更新とは無関係だが、資料性があるため移設先を決めてから削除。 |
| `Portfolio_EditSystem_Proto/` | プログラム（React プロトタイプ） | 編集モード試作環境 | ★★★（高） | 依存が残っている可能性が高い。役割整理と移管手順を確立してから削除。 |
| `tmp/` 配下個別ファイル | 一時ファイル | スクリーンショット／ログなど | ★☆☆（低） | 上位ディレクトリ削除で同時に対応。 |
| `.venv/` | 仮想環境 | ローカル Python 環境 | ★★☆（中） | Node ベースの本プロジェクトには不要。別作業で必要なら残す。 |

> 影響懸念度の目安: ★☆☆＝低、★★☆＝中、★★★＝高。

## 次のアクション例
- 懸念度★☆☆の項目から順次削除し、`docs/refactoring/cleanup-log.md` にログを追記。
- 中〜高懸念の項目は用途ヒアリング・移行計画作成後に削除判断。
- 削除前に `npm run build`, `npm run test` を実行し、影響が無いことを確認する。
