# Issue: ニュースコレクションのフィールド定義と最小バリデーション
> **ステータス:** 完了

## 背景
ニュースコレクションは最初の縦切り対象であり、基本的なフィールドと保存バリデーションを整えることで実運用の準備を進める。

## スコープ
- `EditMode.md` を参照し、ニュース記事に必要なフィールドをDecapのフィールド設定として実装
- 必須項目のバリデーション（タイトル、日付、本文など）と、プレビューに渡すためのデータ整形を作成
- 既存Markdownとの互換性を確認し、必要であれば変換ルールをメモとして追記

## 完了条件
- CMS上でニュース記事を作成・保存した際、必須項目が欠落していると保存がブロックされる
- プレビューに渡されるデータ構造が次Issue（プレビュー実装）で利用できる形になっている

## 依存/備考
- Issue 04の共通レイアウトを前提とする
- リッチテキストや画像の高度なUIは後続Issueに分割し、今回は必須フィールド中心とする
